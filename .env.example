REPLICATE_API_TOKEN=your_api_token

# Vercel Blob — required for the Gallery feature.
# Get this from your Vercel project dashboard → Storage → Blob → your store → .env.local
BLOB_READ_WRITE_TOKEN=your_blob_token
